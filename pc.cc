#include "pc.h"
#include "typedef.h"

PC::getAtk(){
  return atk;
}

PC::getDef(){
  return def;
}

PC::getGold(){
  return gold;
}

PC::getScore(){
  return gold;
}

PC::move(Map& map, Posn p){
  if(! tile.isWalkable()){

  }
}